<div class="container">

<div class="navbar">
<% if user_signed_in? %>
<%= link_to 'Dashboard', dashboard_path, class: 'btn btn-ghost text-xl' %>
<%= link_to 'Accounts', accounts_path, class: 'btn btn-ghost text-xl' %>
<%= link_to 'Transactions', transactions_path, class: 'btn btn-ghost text-xl' %>
<%= link_to 'Categories', categories_path, class: 'btn btn-ghost text-xl' %>
<%= link_to 'Dividends', dividends_path, class: 'btn btn-ghost text-xl' %>
<% end %>

<div class="navbar-end">
<% if user_signed_in? %>
<%= link_to('Logout', destroy_user_session_path, data: { turbo_method: :delete }, class: 'btn btn-ghost text-xl') %>
<% else %>
<%= link_to('Sign Up', new_user_registration_path, class: 'btn btn-ghost text-xl') %>
<%= link_to('Login', new_user_session_path, class: 'btn btn-ghost text-xl') %>
<% end %>
</div>
</div>

</div>
