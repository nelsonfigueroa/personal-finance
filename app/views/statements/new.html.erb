<section>
  <%= render partial: 'layouts/notifications' %>

  <nav aria-label="breadcrumbs">
    <ul>
      <li><%= link_to 'Accounts', accounts_path %></li>
      <li><%= link_to "#{@account.name}", account_path(@account) %></li>
      <li><a href="#" aria-current="page">Add Statement</a></li>
    </ul>
  </nav>

  <h1>Add Statement</h1>

  <%= form_with model: @statement, url: {controller: 'statements', action: 'create'} do |f| %>
    <div>
      <%= f.label :balance %>
      <div>
        <%= f.number_field :balance, step: 0.01, class: '' %>
      </div>
    </div>

    <div>
      <%= f.label :date %>
      <div>
        <%= f.date_field :date, class: '' %>
      </div>
    </div>

    <div>
      <%= f.label :notes %>
      <div>
        <%= f.text_field :notes, class: '' %>
      </div>
    </div>

    <div>
      <%= f.submit "Add", class: '' %>
    </div>
  <% end %>

</section>